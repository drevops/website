{#
/**
 * @file
 * Renders the Fact card paragraph through the drevops:fact-card component.
 */
#}
{% include 'drevops:fact-card' with {
  theme: paragraph.field_c_p_theme.value,
  fact: paragraph.field_c_p_title.value,
  label: paragraph.field_c_p_summary.value,
} only %}
